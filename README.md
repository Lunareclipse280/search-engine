# search-engine
A simple yet functional search engine built entirely with HTML, CSS, and JavaScript. This project demonstrates how client‑side technologies can be used to index and search through a dataset, providing instant results in a clean and responsive interface.
